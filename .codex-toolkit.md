# Codex Toolkit Configuration

Project-specific settings for codex-toolkit commands.
Generated by `/init`. Edit freely — all fields are optional.

## Project

- **Stack**: Static HTML, CSS, JavaScript, JSX (Babel via CDN) — no bundler or build step
- **Test command**: N/A (pure static site)
- **Source directories**: root (`*.html`, `*.css`, `*.js`, `*.jsx`), `storybooks/`

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.5
- **Default effort**: high
- **Default audit type**: mini
- **Default sandbox**: workspace-write

## Audit Focus

balanced

Additional instructions appended to every audit's developer-instructions:

```text
Give equal attention to all audit dimensions.
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
uploads/
.git/
og.png
favicon.svg
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.
Use this to tell Codex about your project's conventions, architecture, or constraints.

```text
This is a pure static site with zero bundler or build step. All pages are self-contained
HTML files linked to styles.css in the root. The design system uses CSS custom properties
defined in styles.css — do not introduce hardcoded color values or new typefaces.
Allowed palette tokens: --coral, --coral-soft, --yellow, --yellow-soft, --green,
--green-soft, --blue, --blue-soft, --cream, --cream-deep, --ink, --ink-soft, --ink-mute,
--paper. Font: "LXGW WenKai TC" only.

The deck-stage.js file is a custom web component; tweaks-panel.jsx is a React component
loaded via CDN Babel. Content must be faithful to source texts in uploads/.
```
