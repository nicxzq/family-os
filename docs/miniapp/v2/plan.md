# 小程序 v2 实施计划（plan）

> 按"难度 × 用户价值 × 依赖"排批次；每批由低成本模型（subagent）实现，批间做验证再继续。批内改动互不跨文件冲突，批间串行。

## 批次

| 批 | 内容 | 需求 | 难度 | 状态 |
|---|---|---|---|---|
| P0 | requirements / spec / plan 三文档 | R10 | — | 本次完成 |
| P1 | 首页文案+互动位置、称呼统一、隐私政策/免责页 | R3 R4 R8 | 低 | 本次执行 |
| P2 | 全页面分享 util + 13 页接入、周会布局/分享/复制上周 | R2 R7 | 中 | 本次执行 |
| P3 | 读本/绘本书架入口、reader/story 上一本下一本 | R5 | 中 | 本次执行 |
| P4 | 游戏中心目录 + 5 个简化原生游戏 + 47 预告 | R6 | 高 | 本次执行 |
| P5 | logo 动图替换 + 抽帧 | R1 | 低 | **等文件**（GIF 入仓库后做） |
| P6 | 云开发内容后端 B1 | R9 | 中 | 规划已出，单独立项 |

## 验证口径（每批）

1. `node --check` 所有改动 .js；wxml 括号/标签目检。
2. 新页面已注册 `app.json`。
3. wxss 无硬编码色值/尺寸（只用 `var(--*)`）。
4. grep 无残留旧称呼（限 §4 范围内文件）。
5. 逐项对照 requirements.md 验收点。

最终需人工在微信开发者工具真机预览：分享卡、canvas 存图授权、游戏交互、周会复制。

## 人工事项（Carl）

- [ ] 把 logo GIF 放进仓库（建议 `miniprogram/assets/logo.gif`），告知文件名 → 触发 P5。
- [ ] 微信后台：《用户隐私保护指引》按 spec §8 填写。
- [ ] R9 立项时：开通云开发环境，把环境 ID 给到实现方。
- [ ] 发版前真机回归 + 提审。

## 风险

- 游戏简化版与网页版体验差异 → 目录页预告文案注明"网站版可玩完整版"。
- 分享到朋友圈（onShareTimeline）个人主体在 iOS 端受限 → 保留实现，实际以微信放量为准。
- 主包体积：5 个游戏 + GIF 需盯 2MB 上限（游戏纯 view/canvas 代码量小，风险低）。
