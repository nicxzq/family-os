.profile {
  display: flex;
  align-items: center;
  gap: var(--space-md);
}

/* 微信原生 button 的默认样式（padding / line-height / min-height）很顽固，
   会把它撑成长方形。所以 button 本身只做无样式包裹，视觉方块交给内部
   固定尺寸的 .avatar-inner——尺寸稳定，圆角正方形。 */
.avatar-btn {
  --avatar-size: 128rpx;
  padding: 0;
  margin: 0;
  border: 0;
  line-height: 1;
  background: transparent;
  flex-shrink: 0;
}

.avatar-btn::after {
  border: none;
}

.avatar-inner {
  width: var(--avatar-size);
  height: var(--avatar-size);
  border-radius: var(--radius-lg);
  overflow: hidden;
  background: var(--cream);
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.avatar-img {
  width: 100%;
  height: 100%;
}

.avatar-placeholder {
  font-size: var(--font-xxl);
  line-height: 1;
}

.profile-body {
  flex: 1;
}

.nickname-input {
  font-size: var(--font-lg);
  font-weight: 700;
  margin-bottom: var(--space-xs);
}

.profile-hint {
  font-size: var(--font-xs);
  color: var(--ink-mute);
}

.row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-md);
}

.row--bare {
  margin-bottom: var(--space-sm);
}

.row-label {
  font-size: var(--font-md);
  font-weight: 700;
}

.row-value {
  font-size: var(--font-sm);
  color: var(--ink-mute);
}

.stats {
  display: flex;
  gap: var(--space-md);
}

.stat {
  flex: 1;
  background: var(--cream);
  border-radius: var(--radius-sm);
  padding: var(--space-sm) var(--space-md);
  text-align: center;
}

.stat-num {
  font-size: var(--font-xl);
  font-weight: 700;
  color: var(--coral);
}

.stat-total {
  font-size: var(--font-sm);
  color: var(--ink-mute);
  font-weight: 400;
}

.stat-label {
  font-size: var(--font-xs);
  color: var(--ink-soft);
}

.records {
  margin-top: var(--space-md);
  border-top: 2rpx dashed var(--cream-deep);
  padding-top: var(--space-sm);
}

.records-empty {
  font-size: var(--font-sm);
  color: var(--ink-mute);
}

.record {
  display: flex;
  align-items: baseline;
  gap: var(--space-sm);
  padding: var(--space-xs) 0;
  font-size: var(--font-sm);
}

.record-kind {
  color: var(--ink-mute);
  font-size: var(--font-xs);
  flex-shrink: 0;
}

.record-title {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.record-date {
  color: var(--ink-mute);
  font-size: var(--font-xs);
  flex-shrink: 0;
}

.help {
  border-top: 2rpx dashed var(--cream-deep);
  padding-top: var(--space-sm);
}

.help-p {
  font-size: var(--font-sm);
  color: var(--ink-soft);
  margin-bottom: var(--space-xs);
}

.about {
  text-align: center;
}

.about-title {
  font-size: var(--font-md);
  font-weight: 700;
  margin-bottom: var(--space-xs);
}

.about-line {
  font-size: var(--font-xs);
  color: var(--ink-mute);
  margin-bottom: var(--space-xs);
}

/* 数据备份 */
.io-desc {
  font-size: var(--font-sm);
  color: var(--ink-soft);
  margin-bottom: var(--space-md);
}

.io-btns {
  display: flex;
  gap: var(--space-sm);
}

.io-btn {
  flex: 1;
  font-size: var(--font-sm);
  color: var(--ink);
  background: var(--cream);
  border: 2rpx solid var(--cream-deep);
  border-radius: var(--radius-full);
  padding: var(--space-xs) 0;
  line-height: 1.6;
}

.io-btn::after {
  border: none;
}

.feedback-btn {
  flex: 1;
  min-width: 0;
  padding: var(--space-sm) var(--space-md);
  background: var(--coral);
  color: var(--paper);
  border: none;
  border-radius: var(--radius);
  font-size: var(--font-sm);
}

.feedback-btn::after {
  border: none;
}

.feedback-btn-ghost {
  background: var(--cream);
  color: var(--ink-soft);
}
